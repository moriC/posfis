<%= form_for(@manage_friend) do |f| %>
  <% if @manage_friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manage_friend.errors.count, "error") %> prohibited this manage_friend from being saved:</h2>

      <ul>
      <% @manage_friend.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :from_user_id %><br>
    <%= f.number_field :from_user_id %>
  </div>
  <div class="field">
    <%= f.label :to_user_id %><br>
    <%= f.number_field :to_user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
